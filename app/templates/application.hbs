<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    {{!-- <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
      data-toggle="collapse" data-target="#menu-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div> --}}
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Kodr</a>
    </div>
    
    <ul class="nav navbar-nav">
      
      <li>
        {{#link-to 'userArenas'}}
          <i class="fa fa-gamepad"></i> User Arenas
        {{/link-to}}
      </li>
      {{#if current_user.atLeastTeacher}}
        <li>
          {{#link-to 'arenas'}}
            <i class="fa fa-gamepad"></i> Arenas
          {{/link-to}}
        </li>
        <li>
          {{#link-to 'concepts'}}
            <i class="fa fa-tags"></i> Concepts
          {{/link-to}}
        </li>
      {{/if}}
    </ul>
    <ul class="nav navbar-nav navbar-right">
      {{#if session.isAuthenticated}}
        <li>
          <a href=""><i class="fa fa-user"></i> <samp>{{current_user.username}}</samp></a>
        </li>
        <li>
          <a href="#" {{action "invalidateSession"}}><i class="fa fa-sign-out"></i> Logout</a>
        </li>
      {{else}}
        <li>
          <a href="#"><i class="fa fa-user-plus"></i> Sign Up</a>
        </li>
        <li>
          <a href="#"><i class="fa fa-sign-in"></i> Login</a>
        </li>
      {{/if}}
      
    </ul>
    </div>
</nav>
<div class="container">
	<h2>Welcome to Kodr</h2>
  	{{outlet}}
    {{login-page store=store}}
    {{#if ready}}
      <div class="row">
        <div class="col-md-6">
          {{ember-chart type="Bar" data=lineData width=1000 options=options colours=colours legend=true height=500}}
        </div>
        <div class="col-md-6">
          {{ember-chart type="Bar" data=lineData width=1000 options=options colours=colours legend=true height=500}}
        </div>
        
      </div>
    {{/if}}
</div>


